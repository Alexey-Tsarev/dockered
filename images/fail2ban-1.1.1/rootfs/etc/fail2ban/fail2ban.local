[Definition]

# Default value in fail2ban.conf
# logtarget = /var/log/fail2ban.log
# override it because a dedicated log folder is better (/var/log/fail2ban)
logtarget = /var/log/fail2ban/fail2ban.log

# loglevel = DEBUG
