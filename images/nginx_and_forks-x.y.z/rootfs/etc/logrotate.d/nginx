/opt/app/log/*.log {
    daily
    missingok
    rotate 365
    compress
    delaycompress
    notifempty
    create 640 root root
    sharedscripts
    postrotate
        if [ -f /run/app.pid ]; then
            kill -USR1 "$(cat /run/app.pid)"
        fi
    endscript
}
